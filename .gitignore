node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.vercel
test-results/
playwright-report/
tests/e2e/.auth/
.tmp-*

# Isolated git worktrees created by Claude Code agents
.claude/worktrees/

# Per-editor MCP configs. `.mcp.json` is tracked deliberately; the Cursor and
# Codex equivalents were removed in 138e611 and their tools keep rewriting them.
.codex/
.cursor/
